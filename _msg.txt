feat(B-1): wire conversation summarization into event pipeline (v1.18.0)

Summary mode now owns ingest by default: per-channel buffer -> idle/scheduled
flush -> LLM summary -> ONE summary engram. Behaviour change: default config
no longer stores one engram per raw message; raw per-message ingest is debug
only (per_message_ingest_debug).

- handlers/format.py: _extract adds chat_type/speaker/group_id/is_bot/peer_*
  stamps; async _resolve_group_name (event.get_group / message_obj.group).
- handlers/event/observe.py: handle_message routes via ConversationBuffer when
  summary mode on; _ingest_per_message legacy path (debug); handle_bot_message
  feeds bot replies; persona prefill provider wired to summarizer.
- main.py: @filter.on_llm_response captures bot reply into buffer; periodic
  idle-flush loop (summary_idle_flush_interval_seconds); terminate flushes +
  cancels the loop.
- service.store_summary: persist one summary engram with identity stamps
  (chat/group/peer tags) + participants + embedding.
- config: summary_idle_flush_interval_seconds (60s).
- smoke v41 (summary-mode end-to-end + debug ingest); updated 20 older smoke
  stubs to expose on_llm_response; v23 field count 105 -> 106.

Refs TODO_summarization_B.md (B-1 wiring).
